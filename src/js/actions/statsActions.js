import {INCREMENT_STR, INCRECMENT_DEX, INCRECMENT_CON, INCRECMENT_INT, INCRECMENT_WIS, INCRECMENT_CHA, DECREMENT_STR, DECREMENT_DEX, DECREMENT_CON, DECREMENT_INT, DECREMENT_WIS, DECREMENT_CHA,INCREMENT_STR_TEMP, INCRECMENT_DEX_TEMP, INCRECMENT_CON_TEMP, INCRECMENT_INT_TEMP, INCRECMENT_WIS_TEMP, INCRECMENT_CHA_TEMP, DECREMENT_STR_TEMP, DECREMENT_DEX_TEMP, DECREMENT_CON_TEMP, DECREMENT_INT_TEMP, DECREMENT_WIS_TEMP, DECREMENT_CHA_TEMP } from '../constants/actionTypes';
export const incrementStr = stat => ({
	type: "INCREMENT_STR",
	payload: stat
});
export const incrementDex = stat => ({
	type: "INCREMENT_DEX",
	payload: stat
});
export const incrementCon = stat => ({
	type: "INCREMENT_CON",
	payload: stat
});
export const incrementInt = stat => ({
	type: "INCREMENT_INT",
	payload: stat
});
export const incrementWis = stat => ({
	type: "INCREMENT_WIS",
	payload: stat
});
export const incrementCha = stat => ({
	type: "INCREMENT_CHA",
	payload: stat
});

export const decrementStr = stat => ({
	type: "DECREMENT_STR",
	payload: stat
});
export const decrementDex = stat => ({
	type: "DECREMENT_DEX",
	payload: stat
});
export const decrementCon = stat => ({
	type: "DECREMENT_CON",
	payload: stat
});
export const decrementInt = stat => ({
	type: "DECREMENT_INT",
	payload: stat
});
export const decrementWis = stat => ({
	type: "DECREMENT_WIS",
	payload: stat
});
export const decrementCha = stat => ({
	type: "DECREMENT_CHA",
	payload: stat
});

export const incrementStrTemp = stat => ({
	type: "INCREMENT_STR_TEMP",
	payload: stat
});
export const incrementDexTemp = stat => ({
	type: "INCREMENT_DEX_TEMP",
	payload: stat
});
export const incrementConTemp = stat => ({
	type: "INCREMENT_CON_TEMP",
	payload: stat
});
export const incrementIntTemp = stat => ({
	type: "INCREMENT_INT_TEMP",
	payload: stat
});
export const incrementWisTemp = stat => ({
	type: "INCREMENT_WIS_TEMP",
	payload: stat
});
export const incrementChaTemp = stat => ({
	type: "INCREMENT_CHA_TEMP",
	payload: stat
});

export const decrementStrTemp = stat => ({
	type: "DECREMENT_STR_TEMP",
	payload: stat
});
export const decrementDexTemp = stat => ({
	type: "DECREMENT_DEX_TEMP",
	payload: stat
});
export const decrementConTemp = stat => ({
	type: "DECREMENT_CON_TEMP",
	payload: stat
});
export const decrementIntTemp = stat => ({
	type: "DECREMENT_INT_TEMP",
	payload: stat
});
export const decrementWisTemp = stat => ({
	type: "DECREMENT_WIS_TEMP",
	payload: stat
});
export const decrementChaTemp = stat => ({
	type: "DECREMENT_CHA_TEMP",
	payload: stat
});

